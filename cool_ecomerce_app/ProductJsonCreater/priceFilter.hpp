#ifndef priceF
#define priceF
#include <vector>
#include <string>
#include "info.hpp"
using namespace std;

int partition(vector<info>& in,int start, int end);
void quickSort(vector<info>& in,int start,int end);

#endif